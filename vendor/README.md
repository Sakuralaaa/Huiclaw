# Vendor Notes

- `nullclaw-source/`: local upstream snapshot used to build the bundled `nullclaw.exe` in CI/release.
- `nullclaw-chat-ui/`: official chat UI snapshot kept for protocol/state reference and future syncs.

Use `scripts/sync-upstream.ps1` to refresh protocol/UI references and update vendor snapshots intentionally.
